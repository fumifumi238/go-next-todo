

services:
  backend:
    # 開発用Dockerfileを使用
    build:
      context: ./backend
      dockerfile: Dockerfile.dev
    # ソースコードをマウントし、airによる自動リロードを有効化
    env_file:
      - .env
    volumes:
      - ./backend:/app
    # 実行コマンドをairに変更
    command: air -c .air.toml
